.menu-list
  %ul.tag-list
    %p タグ
    -@tags.each do |tag|
      %li.tag 
        = link_to tag.tag

  %ul.category-list
    %p カテゴリー
    -@category_parent_array.each do |category|
      %li.category 
        =link_to category_path(category.id) do
          = category.text